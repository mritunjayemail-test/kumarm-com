import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kumarm',
  appName: 'kumarm-com',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
